orking with Remote
==========================
Create PAT (Personal Access Token) -> Go to github.com -> settings -> developer settings -> personal access tokens -> generate -> 
                name: <ANY NAME>
                Tick all the options then generate


Create Repository on github.com.

#git remote add <REMOTE NAME> <GITHUB URL> -> to add remote 
        example: #git remote add fujitsu https://github.com/gauravkumar9130/class
W
#git remote -v -> to list remote urls
#git push <REMOTE NAME> master -> to push code
username:
password: USE PAT TOKEN

Go to github.com and verify 

#git push <REMOTE NAME> --tags -> to push tags


#git clone <URL>        
        example: #git clone https://github.com/gauravkumar9130/gaurav-app
